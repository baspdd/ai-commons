# Deprecated Project Guide

Project architecture does not belong in the shared AI base.

Use the host repository's `.ai/PROJECT.md` as the canonical project guide. This file is
retained only so older adapters fail toward the correct location instead of loading a
stale duplicated architecture document.
